import { NavLink } from 'react-router';

export const InteractiveGallery = () => {
    return (
        <>
            <div>Interactive Gallery</div>
            <NavLink to="/">Back to Gallery</NavLink>
        </>
    );
};